# Telegram MTProto-клиент (слушаем каналы от лица пользователя)
Telethon==1.36.0

# Чтение .env
python-dotenv==1.0.1

# Генерация QR-кода для входа без ввода кода (login_qr.py)
qrcode[pil]==7.4.2

# Бот для отправки результатов в группу (core/notifier.py)
aiogram==3.13.1

# LLM-слой. Провайдер выбирается в .env (LLM_PROVIDER):
#   deepseek  → используется пакет openai (OpenAI-совместимый API DeepSeek)
#   anthropic → используется пакет anthropic
openai>=1.40
anthropic==0.39.0

# HTTP-клиент для CRM Bitrix24 (core/crm.py); обычно уже стоит как зависимость aiogram
aiohttp>=3.9
